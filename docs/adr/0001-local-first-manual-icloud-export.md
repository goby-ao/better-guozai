# 本地优先并通过 iCloud 文件备份恢复

首版以设备上的 SwiftData 数据库作为唯一事实来源，由家长主动将版本化的明文 JSON 和 CSV 导出到系统“文件”或 iCloud Drive，并支持验证、预览后按稳定标识安全合并恢复，不启用 CloudKit 自动同步。明文格式便于直接使用 Excel、Python 等工具分析，应用需要在导出前提示其中包含成长记录和回顾文字。当前产品面向单个家庭且不设账号，自动同步会提前引入账号状态、冲突处理、网络异常和 CloudKit 模型约束；手动文件备份已经满足数据留存与后续分析需求，同时保留未来单独增加多设备同步的空间。
